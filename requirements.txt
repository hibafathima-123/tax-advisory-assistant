# Core RAG framework (langchain.text_splitter, langchain_core.documents)
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.38

# LLM client (OpenAI-compatible SDK, used to call Groq's endpoint)
openai==1.40.0

# Web UI
streamlit==1.37.1

# .env loading for GROQ_API_KEY
python-dotenv==1.0.1

# Required by langchain_community.retrievers.TFIDFRetriever
# NOTE: scikit-learn 1.8.0 requires Python >=3.11, which conflicts with
# this app's stated "Python 3.10+" support (see app.py sidebar). Pinned to
# the 1.5.x line here, which still supports Python 3.9-3.13.
scikit-learn==1.5.2

# Transitive dependencies, pinned explicitly for reproducible installs
numpy==1.26.4
httpx==0.27.2
