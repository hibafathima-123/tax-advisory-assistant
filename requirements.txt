# Core RAG framework (langchain.text_splitter, langchain_core.documents)
langchain==0.2.16
langchain-core==0.2.38

# LLM client (OpenAI-compatible SDK, used to call Groq's endpoint)
openai==1.40.0

# Web UI
streamlit==1.37.1

# .env loading for GROQ_API_KEY
python-dotenv==1.0.1

# Embedding-based semantic retrieval (replaces TF-IDF / scikit-learn)
# sentence-transformers pulls in torch automatically; the CPU-only wheel
# is what pip resolves by default on Windows unless a CUDA index is set,
# which keeps installs simple and avoids GPU/CUDA setup requirements.
sentence-transformers==3.0.1

# Local vector similarity search (FAISS IndexFlatIP)
faiss-cpu==1.8.0.post1

# Transitive dependencies, pinned explicitly for reproducible installs
numpy==1.26.4
httpx==0.27.2
